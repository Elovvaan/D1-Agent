import { ArrowRight, Building2, Search, SlidersHorizontal, Users } from "lucide-react";
import { EntityMark } from "@/components/entity-mark";
import { Badge } from "@/components/design-system";
import { PublicSiteShell } from "@/components/public-site-shell";
import { getPublicDirectoryCounters, searchPublicDirectory } from "@/lib/data/services";

function SchoolCard({ id, title, detail, href, badge }: { id: string; title: string; detail: string; href: string; badge: string }) {
  return <a className="rounded-2xl bg-white p-5 text-[#0A1A3F] shadow-[0_22px_60px_rgba(0,0,0,0.28)] transition hover:-translate-y-1" href={href}><div className="flex items-start justify-between gap-3"><EntityMark entity={{ ref_type: "School", ref_id: id, display_name: title }} kind="logo" size={64} /><Badge tone="green">{badge}</Badge></div><h2 className="mt-4 text-sm font-black">{title}</h2><p className="mt-1 text-xs font-semibold leading-5 text-[#66718F]">{detail}</p><div className="mt-5 flex items-center justify-between border-t border-[#E4E9F1] pt-4 text-xs font-black text-[#1B3FA0]"><span>Open public page</span><ArrowRight size={16} /></div></a>;
}

export default function SchoolsPage() {
  const counters = getPublicDirectoryCounters();
  const schoolResults = searchPublicDirectory("school").find((group) => group.group === "Schools")?.results.slice(0, 12) ?? [];
  return <PublicSiteShell variant="dark"><section className="relative min-h-screen overflow-hidden bg-[#061331] text-white"><div className="absolute inset-0 bg-[radial-gradient(circle_at_80%_20%,rgba(27,63,160,0.55),transparent_34%),linear-gradient(135deg,#061331,#08245B_55%,#061331)]" /><div className="absolute inset-0 opacity-[0.16] [background-image:radial-gradient(circle_at_1px_1px,white_1px,transparent_0)] [background-size:22px_22px]" /><div className="relative mx-auto max-w-[1440px] px-4 py-14 sm:px-6 lg:px-8"><div className="mb-8 flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><div className="text-xs font-black uppercase tracking-[0.28em] text-[#F2C200]">Schools Directory</div><h1 className="mt-4 text-4xl font-black tracking-tight sm:text-5xl">Discover public schools, teams, rosters, and activity.</h1><p className="mt-3 max-w-2xl text-sm font-semibold leading-6 text-[#C8D6FF]">School pages stay public-facing and clean while Operations handles review in the background.</p></div><a href="/get-started" className="inline-flex min-h-12 items-center justify-center gap-2 rounded-xl bg-[#F2C200] px-5 text-sm font-black text-[#0A1A3F]">Add / Claim School <ArrowRight size={16} /></a></div><form className="grid gap-3 rounded-[26px] border border-white/12 bg-white/[0.055] p-4 backdrop-blur md:grid-cols-[1fr_auto_auto]"><div className="flex min-h-12 items-center gap-3 rounded-xl bg-white px-4 text-[#0A1A3F]"><Search size={18} className="text-[#66718F]" /><input className="flex-1 bg-transparent text-sm font-semibold outline-none" name="q" type="search" /></div><button className="rounded-xl border border-white/14 bg-white/[0.08] px-4 text-sm font-black text-white">All Sports</button><button className="inline-flex items-center justify-center gap-2 rounded-xl border border-white/14 bg-white/[0.08] px-4 text-sm font-black text-white"><SlidersHorizontal size={16} /> More Filters</button></form><div className="mt-5 grid gap-4 md:grid-cols-3"><div className="rounded-2xl border border-white/12 bg-white/[0.06] p-5"><Building2 className="text-[#F2C200]" /><div className="mt-3 text-3xl font-black">{counters.schools}</div><p className="text-sm font-semibold text-[#C8D6FF]">Schools indexed</p></div><div className="rounded-2xl border border-white/12 bg-white/[0.06] p-5"><Users className="text-[#F2C200]" /><div className="mt-3 text-3xl font-black">{counters.teams}</div><p className="text-sm font-semibold text-[#C8D6FF]">Teams discovered</p></div><div className="rounded-2xl border border-white/12 bg-white/[0.06] p-5"><Search className="text-[#F2C200]" /><div className="mt-3 text-3xl font-black">Live</div><p className="text-sm font-semibold text-[#C8D6FF]">Public school search</p></div></div><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{schoolResults.map((school) => <SchoolCard key={school.id} id={school.id} title={school.title} detail={school.detail} href={school.href} badge={school.typeLabel} />)}</div>{!schoolResults.length ? <div className="mt-8 rounded-[26px] border border-white/12 bg-white/[0.06] p-6 text-center text-sm font-semibold text-[#C8D6FF]">No reviewed school records are ready yet.</div> : null}</div></section></PublicSiteShell>;
}
